import { AppBar, Toolbar, Typography } from "@mui/material"

function NavBar() {
    return (
        <AppBar>
            <Toolbar>
                <Typography variant="h4">NBA Players</Typography>
            </Toolbar>
        </AppBar>
    )
}

export default NavBar
